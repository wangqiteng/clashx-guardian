# Changelog

## 2.2.2 - 2026-08-31

- 修复 Guardian 启动较慢时重复点击“开启”会反复终止 `xpcproxy`、触发 launchd 节流并进一步延迟启动的问题。
- 启动改为识别运行中、启动中和已停止状态；每次只发送一次非破坏性启动请求，随后最多等待 120 秒并显示进度。
- 启动期间禁用开启、暂停和重启操作，避免并发控制命令互相干扰。
- 新增“打开诊断日志”，持久记录菜单栏及 LaunchAgent 的启动命令、退出码和错误输出，超过 512 KiB 自动轮换。
- 增加 launchd 状态解析、启动决策和可见进度文案的回归测试。

## 2.2.1 - 2026-08-26

- 菜单栏图标改为完整、单色、左右对称的盾牌，仅用盾牌颜色表达网络状态。
- 修复 Retina 位图重复缩放造成盾牌被裁切、视觉重心偏移的问题。
- 状态栏图标使用固定正方形占位，并增加左右镜像与单色构成回归测试。

## 2.2.0 - 2026-08-26

- 菜单栏图标改为彩色盾牌与白色 Wi-Fi 波纹组合，强化网络守护含义并保持状态色辨识。
- 新增同视觉语言的启动台与 Finder 应用图标，并在构建时生成完整的 macOS `AppIcon.icns`。
- 增加图标像素构成、尺寸和应用包资源校验。

## 2.1.0 - 2026-08-26

- 故障确认后使用与 ClashX“延迟测速”菜单相同的 `/proxies/<节点>/delay` 接口，并发测试整组节点；默认最多 64 路并发，超大策略组自动分批。
- 忽略 `0 ms` 等无效测速结果，并补齐测速任务启动失败时的状态进度。
- 停止 Guardian 时立即中断测速与切换流程，避免退出过程中继续更改节点。
- 可用节点严格按本次实测延迟从低到高验证，不再由历史评分压过实时延迟。
- 菜单栏显示全量测速进度、候选节点实时延迟和最终切换延迟。
- 扩展内部测试与真实控制器只读测速自检。

## 2.0.0 - 2026-08-25

- 分离 Codex 主探针和公共外网探针，显示结构化故障原因。
- 达到阈值后增加二次确认，并在多个可测速节点共同失败时停止盲目切换。
- 候选节点改为默认 3 路有限并发测速。
- 持久化节点评分，根据成功率、近期失败和平均延迟排序，并轮换候选窗口。
- 区分上次切换尝试与上次成功切换，失败尝试使用独立短冷却。
- 菜单栏新增诊断、测速进度和最近事件，并优化开启、暂停、重启文案。
- 新增后台自测、只读并发测速测试以及无私人运行数据的分享包流程。
